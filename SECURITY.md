# Security Policy

## 対応バージョン

現在は`0.x`のみを開発対象とします。API互換性より、重大なセキュリティ修正を優先する場合があります。

## 脆弱性の報告

公開IssueへAPIキー、プロンプト、顧客データ、再現用の秘密情報を貼らないでください。GitHubのPrivate vulnerability reportingが利用可能な場合は、リポジトリのSecurityタブから報告してください。

報告には次を含めてください。

- 影響するバージョンまたはコミット
- 再現手順
- 想定される影響
- 秘密情報を除いた最小の証拠

## セキュリティ境界

- 本ルーターはDLP、WAF、認証基盤、秘密管理製品の代替ではありません。
- 自動データ分類は誤検知・見逃しがあり得ます。重要テナントは `allowed_deployments: ["local"]` のような強制ポリシーを使用してください。
- `require_auth: true` とTLS終端を本番の必須条件にしてください。このHTTPサーバー単体はTLSを終端しません。
- APIキーは環境変数または外部Secret Managerから注入し、JSONへ保存しないでください。
- Receiptはプロンプト本文を保存しませんが、タスク種別、モデルID、費用等のメタデータを含みます。監査API自体もアクセス制御してください。
- 応答キャッシュを有効にすると回答本文をSQLiteへ保存します。機密処理では無効のままにしてください。
- 合議用候補は「信頼できないデータ」として最終モデルへ渡しますが、プロンプトインジェクションを完全には排除できません。外部ツールの実行権限はクライアント側で最小化してください。

## 依存関係

実行時の外部Python依存はありません。コンテナのベースイメージとPython標準ライブラリのセキュリティ更新は継続して適用してください。

